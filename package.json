{
  "name": "nuqta-gallery",
  "version": "1.0.0",
  "private": true,
  "description": "Front-end for Nuqta, a contemporary art gallery in Islamabad. PHP templates, Tailwind CSS v4, vanilla JS.",
  "scripts": {
    "css": "tailwindcss -i ./src/input.css -o ./assets/css/tailwind.css --minify",
    "css:watch": "tailwindcss -i ./src/input.css -o ./assets/css/tailwind.css --watch",
    "serve": "php -S localhost:8000",
    "start": "npm run css && npm run serve"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
